export default class DateEvent {
    constructor(id, abbvTitle, fullTitle, events, active, dateRegister) {
        this.id = id
        this.abbvTitle = abbvTitle
        this.fullTitle = fullTitle
        this.events = events
        this.dateRegister = dateRegister
        this.active = active
    }
}